export default function CreditPage() {
  return (
    <main className="mx-auto w-full max-w-2xl px-6 py-14">
      <h1 className="text-3xl font-bold tracking-tight">Credit</h1>
      <p className="mt-3 max-w-md leading-relaxed text-muted">
        No positions yet. This desk is ready for a real credit thesis —
        issuer, instrument, rating, spread, seniority — when there's an
        actual trade to track, not before.
      </p>
    </main>
  );
}
